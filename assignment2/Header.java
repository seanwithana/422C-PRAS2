package assignment2;

public class Header {
    //Does the program need to output to console the whole time? or does it need to only do so when the history is requested?
    //What does it mean that each source file needs its own header (Does that mean its own file or just header at the top)
    //Ask about what the command line argument shows up as, i.e string or integer
    //The number of colors should be changeable for full credit, then it says should be changeable by changing config file.
    //Is game config supposed to be in there?
}

